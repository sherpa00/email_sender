<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            text-align: center;
        }
        h2 {
            color: green;
            font-size: 34px;
            text-decoration: underline;
            text-align: center;
            margin-top: 10px;
            margin-bottom: 10px;
        }

        p {
            color: black;
            font-size: 20px;
            font-weight: 500;
            text-align: center;
        }

        a {
            text-align: center;
            font-size: 20px;
            color: black;
            text-decoration: none;
            cursor: pointer;
        }

        a:hover {
            color: white;
        }

        button {
            background-color: green;
            border-radius: 5px;
            width: 200px;
            padding: 5px;
        }

        ul {
            text-align: center;
            margin: auto;
            display: grid;
            align-items: center;
            justify-content: space-evenly;
            grid-template-columns: auto auto auto;
            width: 90%;
            padding: 20px 10px;
            gap: 20px;

        }

        ul div {
            font-size: 18px;
            background-color: lightgreen;
            padding: 10px;
            border-radius: 10px;
            text-align: left;
            width: 370px;
            height: auto;
            word-break: break-all;

        }

        ul div .del {
            font-size: 18px;
            padding: 25px 10px;
            text-align: center;
            border-radius: 5px;
            width: 200px;
            padding: 5px;
            text-align: center;
            margin: 10px auto;
            background-color: crimson;
            cursor: pointer;
        }

        .del:hover {
            color: white;
        }



    </style>
</head>
<body>
    <h2>
        Welcome To EmailSender
    </h2>
    <p>A simple web app made with node js to send email from one client to another without any hassle.</p>

    <button>
        <a href="/send">
            Send Emails now
        </a>
    </button>

    <h3>
        Here is the list of emails send so far.
    </h3>

    <ul>
        <% emails.forEach((email) => { %>
            <div>
                from : <b><%= email.from %></b> <br></br>
                to : <b><%= email.to %></b> <br></br>
                subject : <b><%= email.subject %></b> <br></br>
                text : <b><%= email.text %></b> <br></br>
    
                <form action="/remove/<%= email._id %>" method="post">
                    <button type="submit" class="del">
                        Delete
                    </button>
                </form>
            </div>
        <% }) %>
        
    </ul>

</body>
</html>